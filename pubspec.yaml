name: kuraklik_riskim
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: ^2.0.6+5
  get: ^4.6.5
  http: ^0.13.5
  image_picker: ^0.8.7+1
  mobx_codegen: ^2.1.1
  shared_preferences: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3

flutter:
  assets:
    - lib/assets/images/
  uses-material-design: true
